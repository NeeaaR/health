import { Button, Card, Typography } from "@mui/material";
import { Box, Stack } from "@mui/system";

export default function Time() {
    return(
        <Button variant="outlined" sx={{ maxWidth: "75px", marginX: 1 }}>7:15</Button>
    )
}